# 部署脚本说明

这一层说明宿主机部署脚本和当前运行边界。项目保留现有小项目结构和各自 venv；编排器与只读监控器已经拆开运行。

## 1. 初始化环境

```bash
cd <自动化直播间项目目录>
./scripts/bootstrap.sh
```

脚本会分别准备：

- `弹幕提取/DouyinLiveWebFetcher/venv`
- `语音生成/.venv`
- `Obs_auto/.venv`

## 2. 准备本机配置

```bash
cp .env.example .env
```

然后编辑 `.env`：

- `LIVE_ID`
- `AIBZ_TOKEN`，不要带 `Bearer ` 前缀
- `DOUBAO_LLM_API_KEY` 和 `DOUBAO_TTS_API_KEY`
- `DANMU_JSONL_GLOB` 或 `DANMU_JSONL`，不填则默认读取项目内弹幕输出目录的最新 JSONL
- `OBS_PASSWORD`
- `OBS_LATEST_GLOB` 或 `OBS_FILE`，不填则默认读取项目内弹幕输出目录的最新 JSONL

`.env` 只放本机配置和密钥，不要提交。

`LIVE_ID` 是直播间 ID 的唯一默认来源。正常情况下只需要改这一处：

- `run_danmu.sh` 用它启动弹幕抓取端。
- `run_pipeline.sh` 用它自动选择 `弹幕提取/DouyinLiveWebFetcher/output/${LIVE_ID}_*.jsonl` 中最新的结构化弹幕文件。
- `run_obs_text.sh` 用它自动选择同一批结构化弹幕文件。

## 3. 启动顺序

终端 1：

```bash
./scripts/run_danmu.sh
```

终端 2：

```bash
./scripts/run_pipeline.sh
```

终端 3：

```bash
./scripts/run_monitor.sh
```

`run_pipeline.sh` 启动的是编排器，负责调用小程序 API、LLM 和 TTS；
`run_monitor.sh` 启动的是只读监控器，只读取 `链路监控/events/*.jsonl`，不调用任何外部业务服务。

终端 4：

```bash
./scripts/run_obs_text.sh
```

## 4. 离线验证

不连 aibz / LLM / TTS：

```bash
PIPELINE_DRY_RUN=1 PIPELINE_FROM_START=1 ./scripts/run_pipeline.sh
```

只验证 OBS 文本读取，不连接 OBS：

```bash
OBS_DRY_RUN=1 OBS_ONCE=1 ./scripts/run_obs_text.sh
```

只读取已有事件流做监控观察：

```bash
MONITOR_FROM_START=1 ./scripts/run_monitor.sh
```

## 5. 当前边界

编排器与监控器已经解耦：

- `链路监控/orchestrator.py` 负责业务编排，调用小程序 API、LLM 和 TTS，并写 `events/` 与 `traces/`。
- `链路监控/monitor.py` 只读取 `events/`，不调用任何外部业务服务。
- `链路监控/pipeline_monitor.py` 保留为兼容入口，内部转调 `orchestrator.py`。
