# 全链路运行手册

本手册说明在一台 Mac 上从零启动整条 **直播间弹幕 → 岗位搜索 → 解说生成 → TTS 音频** 链路的步骤，以及如何在真实环境下检测每一段是否跑通。

---

## 1. 链路全貌

```
[抖音 Web 直播间]
        |  WebSocket
        v
+-----------------------------+
| 弹幕提取                     |
| 弹幕提取/DouyinLiveWebFetcher |
| - 抓弹幕 + 解析意图          |
| - 输出 output/*.jsonl        |
+--------------+--------------+
               |  jsonl 行 (含 query_payload)
               v
+-----------------------------+
| 编排器                       |
| 链路监控/orchestrator.py     |
| - tail -F 弹幕 jsonl         |
| - 调 search_jobs_from_payload|
| - 调豆包 LLM 生成解说        |
| - 调豆包 TTS 合成 mp3        |
| - 输出 events/*.jsonl        |
| - 输出 traces/*.jsonl        |
+--------------+--------------+
               |
               | events JSONL
               v
+-----------------------------+
| 只读监控器                    |
| 链路监控/monitor.py          |
| - 只读取事件流                |
| - 打印实时阶段进展和汇总      |
+-----------------------------+
   ^                ^                ^
   |                |                |
+--+---+   +--------+--------+   +---+----+
| aibz |   | 语音生成 LLM/TTS |   | 音频    |
| 后端 |   |   ARK + BigTTS  |   | 文件    |
+------+   +-----------------+   +--------+
```

主要目录 / 入口的角色：

| 目录 | 角色 | 是否需要常驻 |
|---|---|---|
| `弹幕提取/DouyinLiveWebFetcher/` | 抓弹幕 + 结构化，写 jsonl | ✅ 跑直播时常驻 |
| `小程序API提取岗位信息/` | aibz 搜索接口 SDK，被编排器 import；也可独立测试 | ❌ 库代码 |
| `语音生成/` | 豆包 LLM + TTS 工作流，被编排器 import；也可独立运行 | ❌ 库代码 |
| `链路监控/orchestrator.py` | 编排器，串起整条链路并写 events / trace | ✅ 跑直播时常驻 |
| `链路监控/monitor.py` | 只读监控器，读取 events 展示实时状态 | 可选常驻 |
| `Obs_auto/`、`aibz/` | OBS 自动化、小程序源码逆向，**与本链路无关**，本手册不涉及 |

---

## 2. 一次性环境准备

### 2.1 Python 版本

要求 Python 3.9+。`python3 --version` 确认一下。

### 2.2 准备两个 venv

**venv A：弹幕提取专用**

弹幕端依赖较重（`mini_racer`、`betterproto`、`PyExecJS`），单独装：

```bash
cd /Users/zhoukexuanmac/workspace/自动化直播间/弹幕提取/DouyinLiveWebFetcher
python3 -m venv venv         # 已有则跳过
source venv/bin/activate
pip install -r requirements.txt
deactivate
```

**venv B：语音生成 + 编排器 / 监控器 + 小程序API 共用**

`链路监控/orchestrator.py` 通过 `sys.path` 直接 import `小程序API提取岗位信息/aibz_job_search.py` 和 `语音生成/job_narration_workflow.py`，三者依赖一致（`openai` + `requests`），共用一个 venv：

```bash
cd /Users/zhoukexuanmac/workspace/自动化直播间/语音生成
python3 -m venv .venv        # 已有则跳过
source .venv/bin/activate
pip install -r requirements.txt
deactivate
```

> 后续启动 **编排器 / 监控器** 时一律用 `语音生成/.venv/bin/python`。

### 2.3 拿 aibz access_token

编排器调岗位搜索接口必须有登录态 token。步骤：

1. 用微信开发者工具或微信打开 aibz 小程序，登录
2. 开发者工具 → Network 面板 → 触发任意接口请求
3. 找到请求头 `Authorization: Bearer xxxxxxxx`
4. 复制 `Bearer ` **后面**的字符串

```bash
export AIBZ_TOKEN="<上一步复制的 token>"
```

> token 有有效期。一旦编排器日志出现 `JobSearchError code=301`，就要重新抓。

### 2.4 LLM / TTS 凭证

LLM/TTS Key 从项目根目录 `.env` 或系统环境变量读取，代码里不再保存真实明文 Key。

迁移新电脑时，复制 `.env.example` 为 `.env`，填入 `DOUBAO_LLM_API_KEY` 和 `DOUBAO_TTS_API_KEY`。如果要换成自己的火山方舟 / BigTTS 凭证，按 `语音生成/README.md` "环境变量" 一节配置即可。

---

## 3. 启动顺序（两到三窗口）

### 终端 1：启动弹幕抓取

```bash
cd /Users/zhoukexuanmac/workspace/自动化直播间/弹幕提取/DouyinLiveWebFetcher
source venv/bin/activate

# 改 main.py 里的 live_id (URL 中 live.douyin.com/ 后面那串数字)
# 然后启动
python main.py
```

启动后立刻能看到：

```
[init] 原始弹幕日志: danmu/{live_id}_{时间戳}.txt
[init] 结构化输出 (JSONL): output/{live_id}_{时间戳}.jsonl
[init] LLM 兜底: 关闭
【√】WebSocket连接成功.
```

**记下** 第二行 `output/...jsonl` 的完整路径，下一步要传给编排器。

### 终端 2：启动编排器

```bash
cd /Users/zhoukexuanmac/workspace/自动化直播间/链路监控
source ../语音生成/.venv/bin/activate

# 替换 <上一步记下的 jsonl 路径>
python orchestrator.py \
    --danmu-jsonl /Users/zhoukexuanmac/workspace/自动化直播间/弹幕提取/DouyinLiveWebFetcher/output/<live_id>_<时间戳>.jsonl
```

正常启动后输出：

```
[init] danmu jsonl: .../output/xxxx.jsonl
[init] events:      .../链路监控/events/events_<启动时间戳>.jsonl
[init] traces:      .../链路监控/traces/traces_<启动时间戳>.jsonl
[init] audio dir:   .../链路监控/audio (tts=on)
[init] dry_run:     False
```

之后每收到一条弹幕命中（rule 或 llm 解析出 keyword 的），就会看到 4 行进度日志：

```
[a1b2c3d4] received: user='Alice' keyword='产品经理' city='北京' raw='查北京产品经理 本科'
[a1b2c3d4] searched: count=128 first='产品经理'@'某公司' (430ms)
[a1b2c3d4] narrated [doubao]: 现在打开的是北京的产品经理岗位... (1240ms)
[a1b2c3d4] synthesized: trace_a1b2c3d4...mp3 (38421B, 880ms)
[a1b2c3d4] completed: audio=.../链路监控/audio/trace_a1b2c3d4...mp3
```

### 终端 3：启动只读监控器（可选）

```bash
cd /Users/zhoukexuanmac/workspace/自动化直播间/链路监控
source ../语音生成/.venv/bin/activate
python monitor.py --events "events/events_*.jsonl"
```

监控器只读取 `events/*.jsonl`，不调用 aibz、LLM 或 TTS。它崩掉或重启不会影响编排器继续处理弹幕。

---

## 4. 检测全链路是否跑通

链路 5 个阶段，**每段都有一个明确可验证的产物**。建议按下表依次确认：

| # | 阶段 | 怎么手工触发 | 成功标志 |
|---|---|---|---|
| 1 | 弹幕抓到了 | 在直播间观察聊天区 | `danmu/{live_id}_*.txt` 文件持续追加新行；终端 1 持续打印 `【聊天msg】` |
| 2 | 弹幕被结构化 | 在直播间发一条 "查北京产品经理 本科" | `output/{live_id}_*.jsonl` 出现新行，含 `keyword=产品经理` `city=北京` `query_payload.intention_job=["产品经理"]` |
| 3 | 编排器接到弹幕 | 同上，等几秒 | 终端 2 出现 `[<8位trace>] received: ...`；`events/events_*.jsonl` 新行含 `"stage":"received"` |
| 4 | 岗位搜索成功 | 同上 | 终端 2 出现 `[<trace>] searched: count=<N> first='...' (<ms>)`；events 新行含 `"stage":"searched"` |
| 5 | 解说文案生成 | 同上 | 终端 2 出现 `[<trace>] narrated [doubao]: ...`；events 新行含 `"stage":"narrated"` |
| 6 | TTS 音频落盘 | 同上 | 终端 2 出现 `[<trace>] synthesized: trace_*.mp3`；`链路监控/audio/trace_<trace_id>.mp3` 真实存在；`afplay` 一下能听到 |

### 4.1 监控当前运行状态

实时看事件流：

```bash
cd /Users/zhoukexuanmac/workspace/自动化直播间/链路监控
source ../语音生成/.venv/bin/activate
python monitor.py --events "events/events_*.jsonl"
```

定期看一次终态 trace 跑量统计：

```bash
cd /Users/zhoukexuanmac/workspace/自动化直播间/链路监控
source ../语音生成/.venv/bin/activate
python stats.py "traces/traces_*.jsonl"
```

输出长这样：

```
total traces: 47
completed rate: 89.4%

stage distribution:
  completed                42  (89.4%)
  failed_search             3  (6.4%)
  failed_narration          1  (2.1%)
  failed_tts                1  (2.1%)

duration (ms):
  stage             n      p50      p90      p99      max
  search           47      420      980     1450     1450
  narration        46     1180     1820     2310     2310
  tts              45      710     1080     1420     1420

top errors:
  [3x] JobSearchError code=<num>: 无登录态
  [1x] WorkflowError: Doubao LLM returned non-JSON content: ...
```

### 4.2 离线手工 dry-run（不连任何外部 API）

只想验证**编排器代码结构和日志输出**，不想消耗 token / LLM / TTS 配额：

```bash
cd /Users/zhoukexuanmac/workspace/自动化直播间/链路监控
source ../语音生成/.venv/bin/activate

python orchestrator.py \
    --danmu-jsonl <你的弹幕 jsonl> \
    --dry-run --from-start
```

`--dry-run` 时：search 用 `语音生成/examples/sample_job_response.json` 当 mock；narration 走本地模板；TTS 跳过。每条终态 trace 应该显示 `stage=completed` 但 `tts=null`，events 中会依次出现 `received / searched / narrated / completed`。

---

## 5. 真实环境常见故障排查

### 5.1 弹幕端

| 现象 | 原因 | 处理 |
|---|---|---|
| 终端 1 启动后立刻退出，报 `RuntimeError: ...` | `sign.js` 失效（抖音改前端签名了） | 上游项目 `saermart/DouyinLiveWebFetcher` 拉新版 `sign.js`；或本地手动从 抖音 Web 重新逆向 |
| WebSocket 连不上 | `live_id` 错了，或者直播间已下播 | 浏览器打开 `https://live.douyin.com/<live_id>` 确认能看到直播；`get_room_status` 会打印开播状态 |
| 弹幕 txt 一直在写、output jsonl 始终为空 | 弹幕解析层都是噪声/未命中 | 正常现象。手动发一条 "查北京产品经理 本科" 验证 |
| 写 jsonl 时报 `[结构化输出写入异常]` | 磁盘空间或权限 | `ls -la output/` + `df -h` 排查 |

### 5.2 编排器 — 搜索阶段

| 现象 | 原因 | 处理 |
|---|---|---|
| `failed_search: JobSearchError code=301` | token 过期 / 无效 | 按 §2.3 重抓，重新 `export AIBZ_TOKEN`，重启编排器 |
| `failed_search: JobSearchError code=302` | 该接口要 VIP | 业务侧问题，本工具无解 |
| `failed_search: JobSearchError code=305` | 缺资料 | 用真号在小程序里把"基本资料"页填齐 |
| `failed_search: search returned 0 jobs` | filters 太严没匹配 | 看 trace 里的 `danmu.query_payload`；常见是 `education='985/211'` 等本应该被适配层丢弃的值——若仍在传，确认 `小程序API提取岗位信息/aibz_job_search.py` 里的 `_EDUCATION_PASSTHROUGH` 与 `_adapt_payload_to_filters` 是否到位 |
| `requests.exceptions.ProxyError` / `Operation not permitted` | 终端被代理或沙箱拦截 | 换普通终端跑；或临时 `unset HTTP_PROXY HTTPS_PROXY ALL_PROXY` |
| `code=200` 但 `count=0` | filter 偶尔过严 | 大量出现就检查 keyword 词库；个别条偶发可忽略 |

### 5.3 编排器 — 解说阶段

| 现象 | 原因 | 处理 |
|---|---|---|
| `failed_narration: WorkflowError: Doubao LLM returned non-JSON content: ...` | 模型偶发输出不是 JSON | 个别条可忽略；大量出现换更稳的模型，编辑 `语音生成/job_narration_workflow.py:36 DEFAULT_LLM_MODEL` |
| `failed_narration: WorkflowError: Missing dependency` | venv 没装 `openai` | `pip install -r 语音生成/requirements.txt` |
| `failed_narration: 401` 之类 | LLM Key 无效 | 检查 `.env` 或环境变量里的 `DOUBAO_LLM_API_KEY` |
| narration `source=template` 而不是 `doubao` | 你启动了 `--dry-run`，或代码进了 fallback | dry-run 是预期；其他场景是 bug，保留 trace 后追 |

### 5.4 编排器 — TTS 阶段

| 现象 | 原因 | 处理 |
|---|---|---|
| `failed_tts: ... HTTP 401/403` | TTS App ID / Access Key 不匹配 | 按 `语音生成/README.md` 第 79 行起的提示，从豆包语音控制台（**不是**火山方舟文本模型那边）重新拿 App ID 和 Access Key |
| `failed_tts: Doubao TTS returned no audio chunks` | 文本被审核过滤、或服务侧偶发 | 看 trace 里的 `narration.spoken_text` 是否含敏感词；偶发可重试 |
| 音频生成成功但播放无声 | mp3 编码问题 | `afplay` 一下确认文件有效；`file <mp3>` 看是不是 `Audio file with ID3` 等 |
| 想关掉 TTS 只测搜索+解说 | — | 编排器启动时加 `--no-tts` |

### 5.5 编排器 / 监控器自身

| 现象 | 原因 | 处理 |
|---|---|---|
| 启动报 `ERROR: 没有 access_token` | 既没传 `--access-token` 也没 export | 二选一加上 |
| 编排器启动后再没新 event / trace | 弹幕 jsonl 文件路径错了，或弹幕端长时间没有命中 | `tail -F <jsonl>` 看是不是真的有新行；终端 1 是不是还活着 |
| stdout 进度日志出现但 trace 没写到文件 | `traces/` 目录不可写 | `chmod +w 链路监控/traces/` |
| stdout 进度日志出现但 monitor 没显示 | 监控器读错 events 文件 | 检查 `monitor.py --events "events/events_*.jsonl"` 是否匹配编排器启动时打印的 events 路径 |
| `[crash] orchestrator crash: ...` | 编排器自身异常（非业务异常） | 整段堆栈贴出来定位 |

---

## 6. 关停与清理

- 编排器和监控器都按 **Ctrl+C** 退出；编排器会打印一次 `=== stats ===` 总结
- 单次直播的产物：
  - `弹幕提取/DouyinLiveWebFetcher/danmu/{live_id}_*.txt` — 原始弹幕
  - `弹幕提取/DouyinLiveWebFetcher/output/{live_id}_*.jsonl` — 结构化弹幕
  - `链路监控/events/events_*.jsonl` — 实时阶段事件流
  - `链路监控/traces/traces_*.jsonl` — 端到端 trace
  - `链路监控/audio/trace_*.mp3` — TTS 音频
- 这些文件都是**追加写**，不会自动轮转。每周巡检一次磁盘占用即可。

---

## 7. 速查命令清单

```bash
# 启动弹幕抓取
cd 弹幕提取/DouyinLiveWebFetcher && source venv/bin/activate && python main.py

# 启动编排器
cd 链路监控 && source ../语音生成/.venv/bin/activate && \
  python orchestrator.py --danmu-jsonl <弹幕 jsonl 路径>

# 干跑（不连真实 API）
python orchestrator.py --danmu-jsonl <弹幕 jsonl> --dry-run --from-start

# 实时看事件流
python 链路监控/monitor.py --events "链路监控/events/events_*.jsonl"

# 离线统计
python 链路监控/stats.py "链路监控/traces/traces_*.jsonl"

# 试听某条 trace 音频
afplay 链路监控/audio/trace_<trace_id>.mp3

# 重抓 token 后重启编排器
export AIBZ_TOKEN="<新 token>"
# 重新启动编排器
```
