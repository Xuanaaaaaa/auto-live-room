# 新电脑部署运行全流程

本文档面向第一次接触该项目的人，目标是在一台新的 Windows 电脑上把两套配套项目部署并运行起来：

- `auto-live-room`：自动化直播间主链路，运行在 WSL2 Ubuntu 中。
- `D:\workspace\automation`：微信小程序 UI 自动化项目，运行在 Windows 本地。

推荐部署形态如下：

```text
Windows 宿主机
  |- OBS Studio
  |- 微信开发者工具
  |- Node.js
  |- D:\workspace\automation
  `- WSL2 Ubuntu
       `- /home/<Ubuntu用户名>/auto-live-room
```

两套项目通过一份结构化弹幕 JSONL 目录配套运行：

```text
auto-live-room 生成：
/home/<Ubuntu用户名>/auto-live-room/弹幕提取/DouyinLiveWebFetcher/output/*.jsonl

automation 监听：
\\wsl.localhost\Ubuntu\home\<Ubuntu用户名>\auto-live-room\弹幕提取\DouyinLiveWebFetcher\output
```

`\\wsl.localhost\Ubuntu\...` 在部分机器上也可以写成 `\\wsl$\Ubuntu\...`。

---

## 1. 运行链路说明

完整链路分为四段：

```text
抖音直播间弹幕
  -> WSL 中的弹幕抓取与结构化
  -> WSL 中的岗位 API 查询、豆包 LLM 解说、豆包 TTS 音频
  -> Windows OBS 文本/音频展示

同一份结构化弹幕 JSONL
  -> Windows automation watcher
  -> 微信开发者工具 auto 模式
  -> AIBZ 小程序自动搜索、点击岗位详情、滚动、截图
```

`automation` 不替代主链路里的岗位 API 查询，它是并行消费者：主链路负责 API、口播和 TTS，小程序自动化负责把同一条弹幕查询在小程序 UI 中可视化执行。

---

## 2. 软件要求

### 2.1 Windows 侧

安装以下软件：

1. Windows 10/11，建议开启管理员权限安装软件。
2. WSL2 + Ubuntu。
3. Git for Windows。
4. Node.js，建议 18 LTS 或更高，项目最低要求 Node.js 14+。
5. 微信开发者工具。
6. OBS Studio。
7. AIBZ 小程序项目，并已能编译出 `mp-weixin` 产物目录。

常见路径示例：

```text
automation 项目：D:\workspace\automation
微信开发者工具 CLI：D:/software/微信web开发者工具/cli.bat
AIBZ 小程序编译产物：D:/aibz/dist/build/mp-weixin
```

如果你的 AIBZ 小程序仍使用开发产物，路径可能是：

```text
D:/aibz/dist/dev/mp-weixin
```

该目录内必须存在：

```text
project.config.json
```

### 2.2 WSL2 Ubuntu 侧

进入 Ubuntu 后安装基础工具：

```bash
sudo apt update
sudo apt install -y git python3 python3-venv python3-pip curl netcat-openbsd
```

Python 要求 3.9+，可以检查：

```bash
python3 --version
```

---

## 3. 准备账号、密钥和外部服务

部署前需要准备：

1. 抖音直播间 ID：填入 `LIVE_ID`。
2. AIBZ 登录态 token：填入 `AIBZ_TOKEN`，不要带 `Bearer ` 前缀。
3. 豆包/火山方舟 LLM Key：填入 `DOUBAO_LLM_API_KEY`。
4. 豆包 TTS Key：填入 `DOUBAO_TTS_API_KEY`。
5. 如需 OBS 联动：OBS WebSocket 的端口和密码。

获取 `AIBZ_TOKEN` 的常见方式：

1. 打开微信开发者工具或微信中的 AIBZ 小程序并登录。
2. 在开发者工具 Network 面板触发任意接口。
3. 找到请求头 `Authorization: Bearer xxxxx`。
4. 复制 `Bearer ` 后面的字符串。

如果后续 `pipeline.log` 出现 `JobSearchError code=301`，通常表示 token 过期，需要重新获取并更新 `.env`。

---

## 4. 部署 auto-live-room 主项目

以下命令在 WSL2 Ubuntu 中执行。

### 4.1 拉取代码

```bash
cd /home/<Ubuntu用户名>
git clone https://github.com/Xuanaaaaaa/auto-live-room.git
cd auto-live-room
```

如果是从压缩包拷贝项目，也要确保最终目录类似：

```text
/home/<Ubuntu用户名>/auto-live-room
```

### 4.2 配置 .env

复制模板：

```bash
cp .env.example .env
```

编辑 `.env`：

```bash
nano .env
```

最小必填项：

```bash
LIVE_ID=你的抖音直播间ID
AIBZ_TOKEN=你的aibz_token_不要带Bearer前缀
DOUBAO_LLM_API_KEY=你的豆包LLMKey
DOUBAO_TTS_API_KEY=你的豆包TTSKey
```

建议同时确认这些默认项：

```bash
PIPELINE_NARRATION_MODE=llm
PIPELINE_DRY_RUN=0
PIPELINE_NO_TTS=0
OBS_INPUT_NAME=当前查询
OBS_AUDIO_INPUT_NAME=岗位语音
OBS_PORT=4455
```

如果暂时不接 OBS，`OBS_*` 可以先保持默认。

### 4.3 配置 Windows OBS 联动

如果需要让 WSL 生成的 TTS 音频进入 Windows OBS，建议把音频输出到 Windows 可读目录。

在 `.env` 中增加或修改：

```bash
AUDIO_DIR=/mnt/c/Users/你的Windows用户名/auto-live-room-audio
OBS_HOST=Windows宿主机IP
OBS_PORT=4455
OBS_PASSWORD=你的OBSWebSocket密码
OBS_AUDIO_INPUT_NAME=岗位语音
OBS_AUDIO_PATH_MODE=wsl-to-windows
```

创建音频目录：

```bash
mkdir -p "$AUDIO_DIR"
```

获取 Windows 宿主机 IP：

```bash
cat /etc/resolv.conf | grep nameserver
```

输出类似：

```text
nameserver 172.xx.xx.1
```

把这个 IP 填到：

```bash
OBS_HOST=172.xx.xx.1
```

测试 OBS 端口是否可连：

```bash
nc -vz "$OBS_HOST" "$OBS_PORT"
```

如果失败，检查 Windows OBS 是否打开、WebSocket 是否启用、防火墙是否拦截、端口和密码是否正确。

### 4.4 初始化 Python 依赖

在 `auto-live-room` 根目录执行：

```bash
./scripts/bootstrap.sh
```

该脚本会创建并安装三个 Python 虚拟环境：

```text
弹幕提取/DouyinLiveWebFetcher/venv
语音生成/.venv
Obs_auto/.venv
```

依赖大致包括：

```text
弹幕抓取：requests、betterproto、websocket-client、PyExecJS、mini_racer、openai
语音/编排：openai、websockets、requests
OBS：obsws-python、watchdog
```

---

## 5. 部署 D:\workspace\automation 项目

以下命令在 Windows PowerShell 中执行。

### 5.1 拉取代码

```powershell
mkdir D:\workspace -Force
cd D:\workspace
git clone https://github.com/Xuanaaaaaa/automation.git automation
cd D:\workspace\automation
```

如果项目已经通过其他方式拷贝到 `D:\workspace\automation`，直接进入该目录即可。

### 5.2 安装 Node 依赖

```powershell
node -v
npm install miniprogram-automator
```

当前项目没有必须依赖 `package.json` 的安装流程，核心依赖是 `miniprogram-automator`。代码也会尝试从相邻 AIBZ 小程序项目的 `node_modules` 中加载该依赖，但建议仍在 `D:\workspace\automation` 下安装一次。

### 5.3 初始化本机配置

```powershell
cd D:\workspace\automation
node scripts\init-local-config.js
```

按提示填写：

```text
微信开发者工具 CLI 路径
小程序 mp-weixin 产物路径
微信开发者工具自动化端口
弹幕 JSONL 输出目录
NO_PROXY
```

Windows 示例：

```text
wechatDevtoolsCli=D:/software/微信web开发者工具/cli.bat
wechatMiniprogramProject=D:/aibz/dist/build/mp-weixin
wechatAutoPort=9420
danmuJsonlDir=\\wsl.localhost\Ubuntu\home\<Ubuntu用户名>\auto-live-room\弹幕提取\DouyinLiveWebFetcher\output
noProxy=127.0.0.1,localhost
```

脚本会写入：

```text
D:\workspace\automation\config\local.jsonl
```

该文件是本机配置文件，不提交到 Git。

配置读取优先级：

```text
环境变量 > config/local.jsonl > config/local.json > 代码内默认值
```

因此如果某次临时调试，不想改 `config/local.jsonl`，可以用环境变量覆盖。

### 5.4 微信开发者工具设置

打开微信开发者工具，进入：

```text
设置 -> 安全
```

开启：

```text
服务端口
多端插件服务端口
```

`automation` 启动时会检查 `9420` 端口。如果端口未监听，会自动调用：

```text
cli.bat auto --project <mp-weixin产物路径> --auto-port 9420 --trust-project
```

注意：`cli.bat auto` 会打开一个新的微信开发者工具自动化实例，登录状态可能不和你手动打开的实例共享。脚本会自动使用测试账号登录：

```text
手机号：91231231234
验证码：1111
```

---

## 6. OBS 准备

如果只跑主链路和小程序自动化，可以先跳过 OBS。

如需 OBS 联动：

1. 打开 Windows OBS Studio。
2. 启用 OBS WebSocket。
3. 记录端口，默认通常是 `4455`。
4. 记录 WebSocket 密码。
5. 创建文本源，名称建议：

```text
当前查询
```

6. 创建媒体源，名称建议：

```text
岗位语音
```

媒体源建议设置：

```text
本地文件：先任选一个 mp3 占位
循环：关闭
源激活时重新开始播放：开启
```

源名称必须与 `.env` 中的 `OBS_INPUT_NAME`、`OBS_AUDIO_INPUT_NAME` 完全一致。

---

## 7. 第一次启动验证

建议先按“主链路 -> OBS 旁路 -> automation watcher”的顺序启动。

### 7.1 启动 auto-live-room 主链路

在 WSL2 Ubuntu 中：

```bash
cd /home/<Ubuntu用户名>/auto-live-room
./scripts/start_all.sh
```

脚本会提示：

```text
LIVE_ID [...]
Narration mode: llm/template, 1=llm, 2=template [...]
```

如果 `.env` 已配置，直接回车沿用默认值即可。

启动成功后会有三个后台进程：

```text
danmu     弹幕抓取与结构化
pipeline  岗位 API、LLM、TTS 编排器
monitor   只读事件监控器
```

查看状态：

```bash
./scripts/status.sh
```

查看日志：

```bash
./scripts/logs.sh danmu
./scripts/logs.sh pipeline
./scripts/logs.sh monitor
```

主链路产物路径：

```text
logs/danmu.log
logs/pipeline.log
logs/monitor.log
弹幕提取/DouyinLiveWebFetcher/danmu/{live_id}_*.txt
弹幕提取/DouyinLiveWebFetcher/output/{live_id}_*.jsonl
链路监控/events/events_*.jsonl
链路监控/traces/traces_*.jsonl
链路监控/audio/trace_*.mp3
```

### 7.2 验证弹幕结构化

在直播间发一条测试弹幕：

```text
查北京产品经理 本科
```

成功标志：

```text
logs/danmu.log 出现聊天消息
弹幕提取/DouyinLiveWebFetcher/output/{live_id}_*.jsonl 新增一行
logs/pipeline.log 出现 received / searched / narrated / synthesized / completed
链路监控/audio/ 下生成 trace_*.mp3，或生成到 AUDIO_DIR 指定目录
```

如果还没有真实直播，也可以先只做编排器 dry-run；但 dry-run 需要已有 JSONL 文件：

```bash
PIPELINE_DRY_RUN=1 PIPELINE_FROM_START=1 PIPELINE_NO_TTS=1 ./scripts/run_pipeline.sh
```

### 7.3 启动 OBS 文本旁路

先 dry-run，不连接 OBS：

```bash
OBS_DRY_RUN=1 OBS_ONCE=1 ./scripts/run_obs_text.sh
```

正式更新 OBS 文本源：

```bash
./scripts/run_obs_text.sh
```

### 7.4 启动 OBS 音频旁路

先 dry-run，验证能从 events 找到音频：

```bash
OBS_AUDIO_DRY_RUN=1 OBS_AUDIO_ONCE=1 ./scripts/run_obs_audio.sh
```

正式播放最新一条音频：

```bash
OBS_AUDIO_ONCE=1 ./scripts/run_obs_audio.sh
```

直播时常驻运行：

```bash
./scripts/run_obs_audio.sh
```

`start_all.sh` 不会自动启动 OBS 文本和音频脚本，OBS 旁路需要单独终端常驻。

### 7.5 启动 automation watcher

在 Windows PowerShell 中：

```powershell
cd D:\workspace\automation
node watch-danmu.js
```

如果没有使用 `config/local.jsonl`，也可以临时通过环境变量启动：

```powershell
cd D:\workspace\automation

$env:DANMU_JSONL_DIR = "\\wsl.localhost\Ubuntu\home\<Ubuntu用户名>\auto-live-room\弹幕提取\DouyinLiveWebFetcher\output"
$env:WECHAT_DEVTOOLS_CLI = "D:/software/微信web开发者工具/cli.bat"
$env:WECHAT_MINIPROGRAM_PROJECT = "D:/aibz/dist/build/mp-weixin"
$env:WECHAT_AUTO_PORT = "9420"
$env:NO_PROXY = "127.0.0.1,localhost"

node watch-danmu.js
```

启动成功后会：

```text
连接微信开发者工具 ws://127.0.0.1:9420
自动登录 AIBZ 小程序
监听最新 JSONL 文件
把弹幕字段转成小程序筛选条件
串行执行搜索、点击岗位、滚动详情
写日志、状态文件和截图
```

automation 运行产物：

```text
D:\workspace\automation\test-runs\watch-时间戳\test.log
D:\workspace\automation\test-runs\watch-时间戳\status.jsonl
D:\workspace\automation\test-runs\watch-时间戳\screenshots\*.png
```

watcher 首次启动会从当前最新 JSONL 文件末尾开始监听，不会处理历史行。测试时请先启动 watcher，再发送新的直播间弹幕。

---

## 8. 正式直播推荐启动顺序

### 8.1 Windows

1. 打开 OBS Studio，确认 WebSocket、文本源和媒体源正常。
2. 确认微信开发者工具已安装，安全设置里的两个端口开关已开启。
3. 确认 AIBZ 小程序 `mp-weixin` 产物目录存在。

### 8.2 WSL 主链路

```bash
cd /home/<Ubuntu用户名>/auto-live-room
./scripts/start_all.sh
./scripts/status.sh
```

### 8.3 WSL OBS 旁路

另开 WSL 终端：

```bash
cd /home/<Ubuntu用户名>/auto-live-room
./scripts/run_obs_text.sh
```

再另开 WSL 终端：

```bash
cd /home/<Ubuntu用户名>/auto-live-room
./scripts/run_obs_audio.sh
```

### 8.4 Windows 小程序 UI watcher

另开 PowerShell：

```powershell
cd D:\workspace\automation
node watch-danmu.js
```

### 8.5 发送测试弹幕

```text
查北京产品经理 本科
```

观察成功标志：

```text
WSL danmu.log 有聊天消息
WSL output/*.jsonl 新增结构化记录
WSL pipeline.log 出现 completed
OBS 文本源更新
OBS 媒体源播放岗位语音
微信开发者工具自动搜索岗位并进入详情
automation/test-runs/watch-*/ 下出现日志和截图
```

---

## 9. 关停流程

### 9.1 停止 automation watcher

在 PowerShell 窗口按：

```text
Ctrl+C
```

脚本会停止监听，并等待当前队列任务结束。

### 9.2 停止 OBS 旁路

在对应 WSL 终端按：

```text
Ctrl+C
```

### 9.3 停止主链路

在 WSL 中：

```bash
cd /home/<Ubuntu用户名>/auto-live-room
./scripts/stop_all.sh
```

确认：

```bash
./scripts/status.sh
```

---

## 10. 常用命令速查

### WSL auto-live-room

```bash
cd /home/<Ubuntu用户名>/auto-live-room

./scripts/bootstrap.sh
./scripts/start_all.sh
./scripts/status.sh
./scripts/logs.sh danmu
./scripts/logs.sh pipeline
./scripts/logs.sh monitor
./scripts/stop_all.sh
```

单独启动：

```bash
./scripts/run_danmu.sh
./scripts/run_pipeline.sh
./scripts/run_monitor.sh
```

离线验证：

```bash
PIPELINE_DRY_RUN=1 PIPELINE_FROM_START=1 PIPELINE_NO_TTS=1 ./scripts/run_pipeline.sh
```

OBS：

```bash
OBS_DRY_RUN=1 OBS_ONCE=1 ./scripts/run_obs_text.sh
./scripts/run_obs_text.sh

OBS_AUDIO_DRY_RUN=1 OBS_AUDIO_ONCE=1 ./scripts/run_obs_audio.sh
./scripts/run_obs_audio.sh
```

### Windows automation

```powershell
cd D:\workspace\automation

node scripts\init-local-config.js
npm install miniprogram-automator
node watch-danmu.js
```

单条离线 UI 测试：

```powershell
node loop-search.js --input '{"position":"产品经理","education":"本科","city":"北京"}'
```

批量离线 UI 测试：

```powershell
node loop-search.js --file test-queries.json
```

---

## 11. 环境变量清单

### 11.1 auto-live-room 的 .env

核心变量：

| 变量 | 是否必填 | 说明 |
|---|---|---|
| `LIVE_ID` | 是 | 抖音直播间 ID |
| `AIBZ_TOKEN` | 非 dry-run 必填 | AIBZ token，不带 `Bearer ` |
| `DOUBAO_LLM_API_KEY` | LLM 模式必填 | 豆包/火山方舟文本模型 key |
| `DOUBAO_TTS_API_KEY` | TTS 必填 | 豆包 TTS key |
| `DOUBAO_API_KEY` | 可选 | LLM/TTS 共用 key 的兜底 |
| `PIPELINE_NARRATION_MODE` | 可选 | `llm` 或 `template` |
| `PIPELINE_DRY_RUN` | 可选 | `1` 表示不调用真实 API |
| `PIPELINE_NO_TTS` | 可选 | `1` 表示跳过 TTS |
| `AUDIO_DIR` | 推荐配置 | TTS mp3 输出目录，Windows OBS 联动建议 `/mnt/c/...` |

弹幕和编排输入：

| 变量 | 说明 |
|---|---|
| `DANMU_JSONL` | 指定固定结构化弹幕 JSONL 文件 |
| `DANMU_JSONL_GLOB` | 指定 JSONL 匹配规则 |
| `EVENTS_OUT` | 指定 events 输出文件 |
| `TRACES_OUT` | 指定 traces 输出文件 |
| `MONITOR_EVENTS_GLOB` | 指定 monitor 读取的 events |
| `STARTUP_WAIT_SECONDS` | `start_all.sh` 等待新文件生成的秒数 |

OBS 文本：

| 变量 | 说明 |
|---|---|
| `OBS_HOST` | OBS WebSocket 主机，WSL 连 Windows OBS 时填宿主机 IP |
| `OBS_PORT` | 默认 `4455` |
| `OBS_PASSWORD` | OBS WebSocket 密码 |
| `OBS_INPUT_NAME` | OBS 文本源名称，默认 `当前查询` |
| `OBS_FIELD` | 文本源展示字段，默认 `raw_text` |
| `OBS_WRAP_WIDTH` | 文本换行宽度 |
| `OBS_MAX_LINES` | 最大行数 |
| `OBS_DRY_RUN` | `1` 表示只打印不连接 OBS |
| `OBS_ONCE` | `1` 表示处理一次后退出 |

OBS 音频：

| 变量 | 说明 |
|---|---|
| `OBS_AUDIO_INPUT_NAME` | OBS 媒体源名称，默认 `岗位语音` |
| `OBS_AUDIO_EVENTS_FILE` | 指定固定 events 文件 |
| `OBS_AUDIO_EVENTS_GLOB` | 指定 events 匹配规则 |
| `OBS_AUDIO_PATH_MODE` | WSL 控制 Windows OBS 时用 `wsl-to-windows` |
| `OBS_AUDIO_FROM_START` | `1` 表示从文件开头处理 |
| `OBS_AUDIO_DRY_RUN` | `1` 表示只打印不连接 OBS |
| `OBS_AUDIO_ONCE` | `1` 表示处理一次后退出 |

### 11.2 automation 的配置

推荐写入 `D:\workspace\automation\config\local.jsonl`：

```json
{"wechatDevtoolsCli":"D:/software/微信web开发者工具/cli.bat","wechatMiniprogramProject":"D:/aibz/dist/build/mp-weixin","wechatAutoPort":9420,"danmuJsonlDir":"\\\\wsl.localhost\\Ubuntu\\home\\<Ubuntu用户名>\\auto-live-room\\弹幕提取\\DouyinLiveWebFetcher\\output","noProxy":"127.0.0.1,localhost"}
```

等价环境变量：

| 变量 | 说明 |
|---|---|
| `WECHAT_DEVTOOLS_CLI` | 微信开发者工具 CLI 路径 |
| `WECHAT_MINIPROGRAM_PROJECT` | AIBZ 小程序 `mp-weixin` 产物目录 |
| `WECHAT_AUTO_PORT` | DevTools auto WebSocket 端口，默认 `9420` |
| `DANMU_JSONL_DIR` | 主链路结构化弹幕 JSONL 输出目录 |
| `DANMU_LIVE_ID` | 可选，只监听指定 live_id 前缀文件 |
| `DEDUP_WINDOW_MS` | 去重窗口，默认 `30000` |
| `POLL_INTERVAL_MS` | 轮询间隔，默认 `2000` |
| `AUTOMATION_STATUS_JSONL` | 可选，指定状态事件输出文件 |
| `NO_PROXY` / `no_proxy` | 本地地址绕过代理，建议 `127.0.0.1,localhost` |

---

## 12. 常见问题排查

### 12.1 start_all.sh 启动失败，找不到 JSONL

原因可能是弹幕抓取没有成功生成新文件。

检查：

```bash
./scripts/logs.sh danmu
ls -lt 弹幕提取/DouyinLiveWebFetcher/output | head
```

确认直播间 ID 正确，直播间正在开播。

### 12.2 output JSONL 一直为空

可能是直播间没有命中“查岗”类弹幕。手动发送：

```text
查北京产品经理 本科
```

再看：

```bash
tail -f 弹幕提取/DouyinLiveWebFetcher/output/${LIVE_ID}_*.jsonl
```

### 12.3 pipeline 报 JobSearchError code=301

`AIBZ_TOKEN` 过期或错误。重新获取 token，更新 `.env`，再重启：

```bash
./scripts/stop_all.sh
./scripts/start_all.sh
```

### 12.4 豆包 LLM 或 TTS 报 401/403

检查：

```text
DOUBAO_LLM_API_KEY
DOUBAO_TTS_API_KEY
DOUBAO_TTS_APP_ID
DOUBAO_TTS_RESOURCE_ID
```

如果只想先验证非语音链路：

```bash
PIPELINE_NO_TTS=1 ./scripts/run_pipeline.sh
```

### 12.5 OBS 连不上

检查：

```bash
cat /etc/resolv.conf | grep nameserver
nc -vz "$OBS_HOST" "$OBS_PORT"
```

同时确认：

```text
Windows OBS 已打开
OBS WebSocket 已启用
OBS_HOST 是 Windows 宿主机 IP
OBS_PORT 默认 4455
OBS_PASSWORD 正确
Windows 防火墙未拦截
```

### 12.6 OBS 音频不播放

检查：

```text
AUDIO_DIR 是否是 /mnt/c/... 这类 Windows 可读路径
OBS_AUDIO_PATH_MODE 是否为 wsl-to-windows
OBS_AUDIO_INPUT_NAME 是否与 OBS 媒体源完全一致
OBS 媒体源是否在当前场景中
OBS 混音器是否静音
```

先 dry-run：

```bash
OBS_AUDIO_DRY_RUN=1 OBS_AUDIO_ONCE=1 ./scripts/run_obs_audio.sh
```

如果输出仍是 `/mnt/c/...` 而不是 `C:\...`，说明路径转换未生效，检查 `OBS_AUDIO_PATH_MODE`。

### 12.7 automation 报 DANMU_JSONL_DIR 不存在

在 Windows 文件资源管理器中打开：

```text
\\wsl.localhost\Ubuntu\home\<Ubuntu用户名>\auto-live-room\弹幕提取\DouyinLiveWebFetcher\output
```

如果打不开：

1. 确认 WSL Ubuntu 正在运行。
2. 确认 Ubuntu 用户名和项目目录正确。
3. 尝试替换为 `\\wsl$\Ubuntu\...`。
4. 确认主链路至少启动过一次并创建了 `output` 目录。

### 12.8 automation 连不上微信开发者工具

检查：

```powershell
cd D:\workspace\automation
node scripts\init-local-config.js
```

确认：

```text
WECHAT_DEVTOOLS_CLI / wechatDevtoolsCli 路径存在
WECHAT_MINIPROGRAM_PROJECT / wechatMiniprogramProject 目录存在
project.config.json 存在
微信开发者工具安全设置已开启服务端口和多端插件服务端口
9420 未被其他进程异常占用
NO_PROXY 包含 127.0.0.1,localhost
```

### 12.9 automation 启动但不处理旧弹幕

这是正常行为。`watch-danmu.js` 首次启动会从最新 JSONL 文件末尾开始读，避免重复处理历史弹幕。

测试时请先启动：

```powershell
node watch-danmu.js
```

再发送新的直播间弹幕。

### 12.10 小程序搜索没有截图或没进详情

检查：

```text
D:\workspace\automation\test-runs\watch-*\test.log
D:\workspace\automation\test-runs\watch-*\screenshots
```

常见原因：

```text
小程序页面 class 变化
筛选按钮 .filter-icon 未找到
岗位列表为空
xtx-job-item 或 .job-item 结构变化
登录状态异常
城市/学历/岗位类型选项文本不匹配
```

可以先用单条离线 UI 测试排查：

```powershell
node loop-search.js --input '{"position":"产品经理","education":"本科","city":"北京"}'
```

---

## 13. 新电脑交付检查清单

### Windows

- 已安装 WSL2 Ubuntu。
- 已安装 Git、Node.js、微信开发者工具、OBS Studio。
- 已准备 AIBZ 小程序 `mp-weixin` 产物目录。
- 微信开发者工具已开启服务端口和多端插件服务端口。
- OBS 已启用 WebSocket。
- OBS 已创建 `当前查询` 文本源。
- OBS 已创建 `岗位语音` 媒体源。
- `D:\workspace\automation` 已存在。
- `D:\workspace\automation\node_modules\miniprogram-automator` 已安装。
- `D:\workspace\automation\config\local.jsonl` 已配置。

### WSL2 Ubuntu

- `/home/<Ubuntu用户名>/auto-live-room` 已存在。
- `.env` 已创建并填写 `LIVE_ID`、`AIBZ_TOKEN`、豆包 Key。
- 如需 OBS，`.env` 已填写 `OBS_HOST`、`OBS_PORT`、`OBS_PASSWORD`、`AUDIO_DIR`、`OBS_AUDIO_PATH_MODE=wsl-to-windows`。
- 已执行 `./scripts/bootstrap.sh`。
- `./scripts/start_all.sh` 可启动。
- `./scripts/status.sh` 显示 `danmu`、`pipeline`、`monitor` 运行。
- `output/*.jsonl` 能生成结构化弹幕。
- `pipeline.log` 能看到 `completed`。
- `run_obs_text.sh` 和 `run_obs_audio.sh` 已 dry-run 通过。
- `D:\workspace\automation\node watch-danmu.js` 能连接微信开发者工具并监听弹幕。

---

## 14. 安全注意事项

不要提交或公开以下内容：

```text
auto-live-room/.env
AIBZ_TOKEN
DOUBAO_LLM_API_KEY
DOUBAO_TTS_API_KEY
OBS_PASSWORD
D:\workspace\automation\config\local.jsonl 中的本机私有路径
```

换电脑时应重新配置密钥和本机路径，不要把旧电脑的真实 `.env` 当作公共部署文档传播。
